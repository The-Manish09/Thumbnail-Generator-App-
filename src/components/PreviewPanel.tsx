import type { IThumbnail } from "../assets/assets"

const PreviewPanel = ({thumbnail, isLoading, aspectRatio} : {thumbnail: IThumbnail, isLoading}) => {
  return (
    <div>
      
    </div>
  )
}

export default PreviewPanel
